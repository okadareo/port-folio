<%= render "customers/customers/header" %>
<h2><%= current_customer.name %>さんのマイページ編集画面</h2>
<div class="row-2">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "customers/shared/error_messages", resource: resource %>

    <div class="col-12">
      <div class="card">
        <div class="card-body">
          <div class="field">
            <p><%= f.label :お名前 %>　<%= f.text_field :name %></p>
          </div>
          <div class="field">
            <p><%= f.label :メールアドレス %>　<%= f.email_field :email %></p>
          <div class="field">
            <p><%= f.label :電話番号 %>　<%= f.text_field :phone_number %></p>
          </div>
          <div class="actions">
            <%= f.submit "保存" %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
<%= link_to "Back", :back %>
<%= render "customers/customers/footer" %>
